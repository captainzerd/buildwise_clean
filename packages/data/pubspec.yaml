name: buildwise_data
description: Data layer implementations for BuildWise
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  buildwise_domain:
    path: ../domain

dev_dependencies:
  test: ^1.26.3